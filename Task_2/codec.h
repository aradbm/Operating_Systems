#include <stdio.h>
#include <string.h>
#include <string.h>
#include <ctype.h>
#include <stdlib.h>
#define N 1024

int encode(char * src, char * dst, int len);
int decode (char * src, char * dst, int len);
// int decodetwo(char * src, char * dst, int len);
// int encodetwo(char * src, char * dst, int len);
