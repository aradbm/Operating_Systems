#include <stdio.h>
#include <string.h>
#include <unistd.h>
#include <ctype.h>
#include <stdbool.h>
char *encode(char *string)
{
    char *changed = "";
    strcpy(changed, string);
    return changed;
}
char *decode(char *string)
{
    char *changed = "";
    strcpy(changed, string);
    return changed;
}